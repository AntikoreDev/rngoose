/**
 * Converts an string into a seed
 * @param {string} string - String to be converted into seed
 * @returns {number} Number that can be used as seed
 */
declare function seed(string: string): number;
export default seed;
